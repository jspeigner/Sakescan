-- Sync-only placeholder: version recorded on remote database before this repo’s migration set.
-- Does not change schema when applied on an empty DB; on linked DB this row already exists in migration history.
SELECT 1;
